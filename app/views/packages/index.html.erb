<div class="row">
  <div class="span12">
    <div class="widget stacked ">
      <div class="widget-header">
        <i class="icon-briefcase"></i>
        <h3>List Boxes and Items</h3>
      </div> <!-- /widget-header -->
      <div class="widget-content">

        <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered" id="admin_users">
          <thead>
            <tr>
              <th>Box Name</th>
              <th>Item Name</th>
              <th class="td-actions">Action</th>
            </tr>
          </thead>
          <tbody>
            <% @packages.each do |package| %>
              <% package.items.each do |item| %>
                <tr>
                  <td><%= package.reference_id %></td>
                  <td><%= item.name %></td>
                  <td class="td-actions">
                    <% if package.state == "virgin" %>
                      <%= link_to "Retrieve this box", retrieval_requests_path(user_id: current_user.id, package_id: package.id, status: "new"), :method => :post, :class => 'btn btn-primary'%>
                    <% else %>
                      <span class="label label-warning">Box Retrieval in progress</span>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
